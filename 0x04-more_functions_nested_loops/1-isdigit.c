#include "main.h"

/**
 * _isdigit - checks if the char is a digit
 * @c: int to be checked
 * Return: 1 if c is a digit, 0 otherwise
 */
int _isdigit(int c)
{
	char j;
	int num = 0;

	for (j >= '0'; j <= '9'; a++)
	{
		if (j == c)
			num = 1;
	}

	return (num);
}
