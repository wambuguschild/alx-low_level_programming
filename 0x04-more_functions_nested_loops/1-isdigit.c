#include "main.h"

/**
 * _isdigit - checks if the char is a digit
 * @c: int to be checked
 * Return: 1 if c is a digit, 0 otherwise
 */
int -isdigit(int c)
{
	int c = 0;

	if c >= '0' && c <= '9')
		c = 1;
	
	return (0);
}
